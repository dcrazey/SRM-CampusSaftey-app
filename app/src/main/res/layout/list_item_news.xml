<?xml version="1.0" encoding="utf-8"?>
<!-- This layout defines a single row in our news feed RecyclerView -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:background="@drawable/card_background"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title of the incident/post -->
    <TextView
        android:id="@+id/newsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Incident Title"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Description of the incident -->
    <TextView
        android:id="@+id/newsDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="This is a description of the incident that occurred near the library. Students are advised to be cautious."
        android:textColor="@color/text_secondary"
        android:textSize="14sp" />

    <!-- Timestamp of the post -->
    <TextView
        android:id="@+id/newsTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Posted 5 minutes ago"
        android:textColor="@color/accent_blue"
        android:textSize="12sp"
        android:textStyle="italic" />

</LinearLayout>

