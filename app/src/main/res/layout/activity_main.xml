<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:padding="24dp"
    tools:context=".MainActivity"> <!-- Corrected context -->

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Campus Safety"
        android:textColor="@color/text_primary"
        android:textSize="32sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/subtitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleTextView"
        android:text="Your personal safety companion."
        android:textColor="@color/text_secondary"
        android:textSize="16sp" />

    <!-- Main SOS Button -->
    <Button
        android:id="@+id/sosButton"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_centerInParent="true"
        android:background="@drawable/button_primary_red"
        android:elevation="12dp"
        android:gravity="center"
        android:text="SOS"
        android:textColor="@color/white"
        android:textSize="60sp"
        android:textStyle="bold" />

    <!-- Helper Text -->
    <TextView
        android:id="@+id/helperTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/sosButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Press Volume Down 3 times for silent alert"
        android:textColor="@color/text_secondary"
        android:textSize="14sp" />

    <!-- Bottom Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/contactsButton"
            style="@style/Widget.Material3.Button.OutlinedButton.Icon"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="70dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            app:backgroundTint="@color/background_card_light"
            android:text="My Contacts"
            android:textColor="@color/text_primary"
            app:icon="@drawable/ic_contacts"
            app:iconGravity="top"
            app:iconTint="@color/icon_tint"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/feedButton"
            style="@style/Widget.Material3.Button.OutlinedButton.Icon"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="70dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            app:backgroundTint="@color/background_card_light"
            android:text="Campus Feed"
            android:textColor="@color/text_primary"
            app:icon="@drawable/ic_feed"
            app:iconGravity="top"
            app:iconTint="@color/icon_tint"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"/>

    </LinearLayout>

</RelativeLayout>

